# Copy this file to .env.local and fill in real values.
# .env.local is git-ignored and must NEVER be committed.

# ── Media API ────────────────────────────────────────────────────────────────
# Public API URL (used client-side by Next.js)
NEXT_PUBLIC_API_URL=https://your-api-gateway-url/prod

# ── Anthropic AI (region lookup) ─────────────────────────────────────────────
# Required for the /api/region-lookup endpoint.
# Obtain from: https://console.anthropic.com/settings/keys
# Server-side only — NEVER use NEXT_PUBLIC_ prefix here.
ANTHROPIC_API_KEY=sk-ant-replace-with-your-key

# ── Admin authentication ──────────────────────────────────────────────────────
# Server-side only (no NEXT_PUBLIC_ prefix)
# Use a strong, unique password (min 16 characters recommended)
ADMIN_PASSWORD=replace-with-strong-password

# Random secret used to sign the admin session cookie.
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ADMIN_SESSION_SECRET=replace-with-random-64-char-hex-string
